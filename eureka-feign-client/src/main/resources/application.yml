spring:
  application:
    name: eureka-feign-client
server:
  port: 8765
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
##feign自带hystrix,配置方式开启
feign:
  hystrix:
    enabled: true
